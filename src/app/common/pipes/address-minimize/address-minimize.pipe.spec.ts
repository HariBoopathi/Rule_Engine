import { AddressMinimizePipe } from './address-minimize.pipe';

describe('AddressMinimizePipe', () => {
  it('create an instance', () => {
    const pipe = new AddressMinimizePipe();
    expect(pipe).toBeTruthy();
  });
});
